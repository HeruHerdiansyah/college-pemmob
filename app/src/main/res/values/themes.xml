<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Assignment" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent_emerald</item>
        <item name="colorPrimaryDark">@color/accent_emerald_dark</item>
        <item name="colorAccent">@color/accent_emerald</item>
        <item name="android:statusBarColor">@color/bg_dark_primary</item>
        <item name="android:navigationBarColor">@color/bg_dark_primary</item>
        <item name="android:windowBackground">@color/bg_dark_primary</item>
    </style>

    <!-- Splash Theme - fullscreen -->
    <style name="Theme.Assignment.Splash" parent="Theme.Assignment">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>