# Contoh konfigurasi database
# Copy file ini menjadi env.properties lalu sesuaikan nilainya

# Untuk Android Emulator (Android Studio): gunakan 10.0.2.2
# Untuk HP fisik (USB/WiFi): gunakan IP LAN PC (cek via ipconfig), contoh: 192.168.1.100
DB_HOST=10.0.2.2
DB_PORT=3306
DB_NAME=college_smartzoo
DB_USER=root
DB_PASSWORD=password
